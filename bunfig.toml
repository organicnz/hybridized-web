[install]
# Use exact versions for reproducibility
exact = false
frozen = false

[install.cache]
# Enable caching
dir = "~/.bun/install/cache"
disable = false

[run]
# Auto-install missing packages on run
bun = true

[test]
# Test configuration
timeout = 10000
